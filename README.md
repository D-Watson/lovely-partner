
  # Virtual Partner App
  The original project is created by the `figma make: https://www.figma.com/make/`, a convinient prototype and 
  code generated tool.


  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  